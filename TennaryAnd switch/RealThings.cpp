#include <iostream>
using namespace std;
int main(){
    int a;
    if(3+2%5)
    cout<<"this works"<<endl;
    if(a=10)
    cout<<"even this works"<<endl;
    if(-8) 
     // if we put 0 in place of -5 the next thing is not print
    cout<<"suprisingly even this works";
}